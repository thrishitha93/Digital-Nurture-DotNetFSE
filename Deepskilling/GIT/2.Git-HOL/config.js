// Feature branch practice file
// Create different versions on different branches

const config = {
  appName: "My App",
  version: "1.0.0",
  environment: "development"
};

module.exports = config;
